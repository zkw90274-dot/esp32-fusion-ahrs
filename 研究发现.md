# 研究发现

## 项目信息
- **项目名称**：Fusion ICM-42688-P AHRS
- **芯片型号**：ESP32-S3
- **trace_id**：`icm42688-fifo-zero-20260812-r1`
- **创建日期**：2026-08-12
- **最后更新**：2026-08-12

## 技术发现

### 2026-08-12 - FIFO partial-read 约束
- **关联轮次**：Round 1 / Scout
- **查询工具**：本地 PDF 原页渲染与文本提取
- **查询关键词**：`FIFO_RESUME_PARTIAL_RD`, `FIFO_DATA`, `FIFO_COUNT_REC`
- **关键发现**：
  - `FIFO_CONFIG1.bit6=0` 要求重新读取整个 FIFO；bit6=1 才允许分段读取并续读。
  - `0x2F` 的 bit6 为 0，bit3 为 1；现有注释把 bit3 误写成 partial resume。
  - FIFO count 记录模式下标准六轴包为 16 字节，FIFO_DATA 是流式数据端口。
- **来源**：`ICM-42688-P_规格书_WJ56255.PDF` 第 69、74、82 页
- **与本项目关联**：应用每次最多读取 10 包，必须启用 bit6，或改成单事务读取全部 count。
- **可信度**：官方数据手册
- **状态**：已采纳，待实机验证

### 2026-08-12 - 官方 Raw AG FIFO 读取方式
- **关联轮次**：Round 1 / Scout
- **查询工具**：本地官方示例源码与软件指南 PDF
- **查询关键词**：`inv_icm426xx_get_data_from_fifo`, `MPUREG_FIFO_DATA`, `RAW AG EXAMPLE`
- **关键发现**：
  - 非 I3C 路径一次读取 `packet_size * packet_count`，不限制为应用输出数组大小。
  - 官方配置使用 record count、小端 count、STOP-on-FULL、时间戳和 FIFO 水印。
  - 官方 SPI HAL 在一个 CS 事务内发送读地址和全部 dummy 字节，再跳过首个地址阶段 RX 字节。
- **来源**：`Icm426xxDriver_HL.c:996`, `Icm426xxDriver_HL.c:1597`, `spi_master.c:278`，软件指南第 21 页
- **与本项目关联**：当前 ESP-IDF command phase 等价于官方地址阶段，但当前读取数量只覆盖 FIFO 的一部分。
- **可信度**：官方示例
- **状态**：已采纳，待实机验证

## 排查线索

### 2026-08-12 - FIFO_DATA 返回全零
- **错误现象**：SPI 返回 `ESP_OK`，FIFO count 递减，但日志只显示前 16 字节全零且 parser 无有效包。
- **已排除**：WHO_AM_I 和直接传感器寄存器读取正常；SPI 事务已不再卡死；WDT 已恢复。
- **尚未证明**：完整 RX 块是否全零；FIFO 数据阶段 MISO 是否电气上持续为低。
- **根因分析**：已确认存在 partial-read 协议错误；它是必须修复的缺陷，但实机验证前不能声称它是全零的唯一根因。
- **下一证据门**：统计完整 RX 块、列出每个记录 header，并在同一日志周期读取直接传感器寄存器作 MISO 对照。

### 2026-08-12 - Partial-read 修复未消除 FIFO_DATA 全零
- **关联轮次**：Round 1 / Builder
- **实验变量**：仅把实际 FIFO 配置从 `0x2F` 改为 `0x6F`，并将刷新后等待从 200 ms 缩短为 10 ms；保留现有 ESP-IDF SPI command phase
- **构建证据**：主机 parser 测试通过；ESP-IDF v5.4.3 全量构建退出码 0；`build/Fusion.bin` 为 244640 字节；COM18 烧录及 hash 校验通过
- **实机证据**：`FIFO_CONFIG1=0x6F`；连续多次 FIFO chunk 的 `nonzero=0`、`or=0x00`、header 全 `0x00`；同周期 14 字节直接读取有 8-11 字节非零
- **结论**：partial-read 配置错误已修正，但不是全零的唯一根因。现有证据只能证明 `FIFO_DATA` 事务的数据接收阶段全零，不能证明 FIFO SRAM 中写入的是零
- **数据手册反证**：空 FIFO 的数据端口值应为 `0xFF`，当前 `0x00` 不是合法空 FIFO 表现
- **状态**：Round 1 主故障验证失败，回退 RESEARCH
- **下一证据门**：逻辑分析仪同时核对 CS、SCLK、MOSI 首字节 `0xB0`，以及随后 MISO 是否持续为低
- **证据日志**：`tmp/verification/idf_flash_monitor.log`

### 2026-08-12 - PDF 原页与 ESP-IDF v5.4.3 事务语义复核
- **关联轮次**：Round 2 / Researcher
- **查询工具**：本地 PDF 原页渲染、ESP-IDF v5.4.3 本地源码/文档
- **关键发现**：
  - 数据手册第 69 页确认 `FIFO_DATA(0x30)` 是 FIFO data port，复位值为 `0xFF`。
  - 数据手册第 74 页确认 `INTF_CONFIG0=0x50` 对应 record count、小端 count、大端 sensor data；无保留位误写。
  - 数据手册第 82 页确认 `FIFO_RESUME_PARTIAL_RD` 为 bit6，当前 `0x6F` 与分批读取契约一致。
  - 软件指南第 21 页确认 Raw AG 示例使用 FIFO；具体读取行为由配套官方源码给出。
  - ESP-IDF 文档确认 command phase 期间不采样 MISO，`rx_buffer` 只接收 data phase；DMA 缓冲不满足条件时驱动默认会自动重分配并复制。
- **来源**：`tmp/pdfs/datasheet_p69-069.png`, `datasheet_p74-074.png`, `datasheet_p82-082.png`, `userguide_p21-21.png`；ESP-IDF `spi_master.h` 与 `spi_master.rst`
- **关联判断**：现有 command phase 用法符合 ESP-IDF 文档，不能直接定罪；但官方 Atmel HAL 使用单个普通全双工缓冲区发送 `0xB0 + N` 字节，这仍是尚未实机对照的底层差异
- **可信度**：官方数据手册、官方软件指南、ESP-IDF v5.4.3 本地文档
- **状态**：已验证资料语义；计划用专用 FIFO 读取函数做单变量对照

### 2026-08-12 - 官方同形态 SPI 帧对照失败
- **关联轮次**：Round 2 / Builder -> Verifier
- **单一变量**：仅将 `FIFO_DATA` 改为普通全双工帧 `[0xB0][N 个 0x00]`，接收 `N+1` 字节后丢弃 `rx[0]`；普通寄存器仍使用 command phase
- **静态证据**：parser 测试通过；`git diff --check` 通过；ESP-IDF v5.4.3 全量构建退出码 0；独立 Verifier 未发现长度、DMA 对齐、CS 或首字节偏移缺陷
- **实机证据**：COM18 烧录 hash 通过；连续五次 FIFO chunk 仍为 `nonzero=0`、header 全 `0x00`、`valid=0`；同周期直接读取仍有 8-9/14 字节非零；无 WDT
- **结论**：ESP-IDF 独立 command phase 不是 FIFO 全零根因；该实验代码已撤销，避免保留无效分支
- **证据日志**：`tmp/verification/round2_serial.log`
- **下一假设**：仅复制官方 accel/gyro 分步上电与两次 200 us 等待，不改变 SPI 帧、FIFO 配置或解析

### 2026-08-12 - 官方分步上电对照失败
- **关联轮次**：Round 3 / Builder -> Verifier
- **单一变量**：仅将初始化的 `PWR_MGMT0=0x0F` 一次写入改为官方 Raw AG 示例同顺序的 accel LN 读改写、200 us 等待、gyro LN 读改写、200 us 等待；保留原有 100 ms 预热
- **静态证据**：parser 以 `-Wall -Wextra -Werror` 重新编译并通过；`git diff --check` 通过；ESP-IDF 生成的 Ninja 构建退出码 0，生成 244912 字节 `Fusion.bin`；独立审查确认正常路径的 mode mask、保留位和两次 200 us 时序正确，并要求补强 RMW 读取失败处理
- **实机证据**：COM18 烧录及 hash 校验通过；5/5 FIFO chunk 的 `nonzero=0`、`or=0x00`、合法 header 为 0；4 次完整同周期直接读取为 9-10/14 字节非零；无 WDT
- **结论**：一次性同时上电不是 FIFO 全零根因。现有证据已跨越 command phase、官方全双工帧和官方上电时序，继续改寄存器没有新增证据支撑
- **处置**：保留官方分步上电作为初始化一致性修正；RMW 读取失败错误路径经独立复核已解决，但 FIFO 主故障未修复；下一步只接受逻辑分析仪波形或替换传感器 A/B 对照作为新证据
- **证据日志**：`tmp/verification/round3_build.stdout.log`, `round3_flash.stdout.log`, `round3_serial.log`
