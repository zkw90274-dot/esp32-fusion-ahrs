# 编辑清单

## 2026-08-12 - FIFO 全零修复准备
- **trace_id**：`icm42688-fifo-zero-20260812-r1`
- **轮次/角色**：`Round 1 / Scout -> Builder`
- **修改文件**：`项目规划清单.md`, `编辑清单.md`, `硬件资源表.md`, `研究发现.md`
- **操作类型**：新增
- **操作内容**：建立任务恢复、硬件基线、研究证据和编辑追踪文件
- **关联模块**：ICM-42688-P SPI FIFO
- **验证标准**：四份文件包含 trace、轮次、停止条件和可回链证据
- **验证结果**：通过
- **Git提交**：无（用户工作树包含未提交改动，未擅自存档）
- **远端同步**：未 push
- **回档记录**：无

## 2026-08-12 - Round 1 实施与实机验证
- **trace_id**：`icm42688-fifo-zero-20260812-r1`
- **轮次/角色**：`Round 1 / Builder`
- **修改文件**：`main/icm42688.c`, `main/hello_world_main.c`
- **操作类型**：修改
- **操作内容**：将实际初始化路径的 `FIFO_CONFIG1` 改为 `0x6F`，启用部分读取续传；刷新后等待改为 10 ms；增加配置校验、完整 RX 块统计、header 列表和同周期直接寄存器对照
- **验证标准**：主机 parser 测试、ESP-IDF v5.4.3 build、COM18 烧录及实机 FIFO 数据有效性
- **验证结果**：构建和烧录通过；`FIFO_CONFIG1=0x6F` 回读通过；FIFO 完整 RX 块仍为全零，合法包数为 0，主故障未修复
- **证据日志**：`tmp/verification/idf_flash_monitor.log`
- **Git提交**：无（工作树包含用户既有未提交改动）
- **远端同步**：未 push
- **回档记录**：无

## 2026-08-12 - Round 2 官方 SPI 帧单变量对照
- **trace_id**：`icm42688-fifo-zero-20260812-r1`
- **轮次/角色**：`Round 2 / Builder -> Verifier`
- **操作内容**：临时增加官方同形态 full-duplex framed read，并仅用于 `FIFO_DATA`
- **验证结果**：构建、烧录通过，但 FIFO 完整 RX 块仍全零；假设被证伪
- **处置**：撤销本轮 framed-read 代码，保留构建、烧录、串口日志
- **证据日志**：`tmp/verification/round2_build.stdout.log`, `round2_flash.stdout.log`, `round2_serial.log`
- **Git提交**：无（失败实验不存档）

## 2026-08-12 - Round 3 官方分步上电对照
- **trace_id**：`icm42688-fifo-zero-20260812-r1`
- **轮次/角色**：`Round 3 / Builder -> Verifier`
- **修改文件**：`main/icm42688.c`, `项目规划清单.md`, `编辑清单.md`, `硬件资源表.md`, `研究发现.md`
- **操作内容**：将初始化中的一次性 `PWR_MGMT0=0x0F` 改为官方示例同顺序的 accel LN 读改写、等待 200 us、gyro LN 读改写、等待 200 us；RMW 使用可返回 `esp_err_t` 的读取接口，失败立即终止初始化；SPI、FIFO 配置、parser 和工作模式未改
- **验证标准**：合法 `0x68/0x6C` 类 header、`valid>0`、直接读取非零且无 WDT
- **验证结果**：parser、diff check、ESP-IDF 构建与 COM18 烧录通过；5/5 FIFO 块仍全零，合法 header 为 0；直接寄存器读取非零，无 WDT，主故障未修复；独立复核确认 RMW 错误路径修正完成，无剩余代码阻断项
- **证据日志**：`tmp/verification/round3_parser_run.log`, `round3_build.stdout.log`, `round3_flash.stdout.log`, `round3_serial.log`
- **处置**：保留官方分步上电以修正初始化一致性；独立审查发现并修复 RMW 读取失败会静默关闭 accel 的错误路径；停止无新增证据的软件配置试错，转逻辑分析仪或替换传感器对照
- **Git提交**：无（工作树包含用户既有未提交改动，且主故障未修复）
- **远端同步**：未 push
- **回档记录**：无
